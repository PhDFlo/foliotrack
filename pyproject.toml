[project]
name = "ETF-Optmizer"
version = "1.0.0"
description = "Compute DCA ETF deposit equilibrium based on existing investment"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "numpy",
    "pyscipopt",
    "cvxpy",
    "gradio",
    "pandas",
    "matplotlib",
    "pyQt6",
    "yfinance"
]
